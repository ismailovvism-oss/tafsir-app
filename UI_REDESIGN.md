# UI_REDESIGN.md — редизайн интерфейса (утверждённый план)

Статус на 2026-08-07: **ЭТАПЫ 1–2 И ПОРЦИЯ 1 ЭТАПА 3 РЕАЛИЗОВАНЫ в index.html
(v4.5) и media.js (v19)** — см. CHANGELOG 2026-08-07 (три записи). Не
закоммичено. Порция 1 этапа 3: повторы N (2–1000) + пауза «повторить за
чтецом» в плеере; tanzil-бокс перевода в мусхафе (`renderMtrBox`,
`ST.mushafTr`); отдельный размер шрифта тафсиров (`ST.fsTaf`, класс `.taf`).
Порция 2 готова (запушена): Study Mode (`openStudy`, вкладки Тафсиры/Слова/
Моё, вход из листа аята), клавиатура (←/→/пробел/Esc), копирование диапазона.
Порция 3 готова: «По памяти» в hrStart (`hrHide`/`hrVeil`: аят скрыт, тап —
сверка, тап — дальше), полировка (:active, :focus-visible, reduced-motion).

**РЕДИЗАЙН ЗАВЕРШЁН (этапы 1–3).** Отложено до обкатки: дедуп «Аудио и
чтецы» из ⋯ (сначала перенести «Новый набор записей» в плеер); возможная
пятисекционная навигация (ChatGPT-вариант) — отдельным решением после
отзывов пользователей.

Отступления от спеки в реализации этапа 1 (осознанные):
- В меню ⋯ ОСТАВЛЕН пункт «Аудио и чтецы» (спека предлагала убрать): панель 🎧
  — единственный вход к «➕ Новый набор записей» и «Слушать текущую суру»;
  убирать вход до переноса этих функций в плеер (этап 2) = потеря функций.
- Переключатель режимов (6 иконок) не тронут — уходит в этапе 2; подписи на
  узких экранах по-прежнему скрыты (лечится сокращением режимов, не шрифтом).
- Desktop hover-раскрытие не делалось: кнопок в панельке осталось 3 тихих SVG —
  прятать их за hover уже нечего.

Кликабельный макет (Artifact, обновляется по `url`):
**https://claude.ai/code/artifact/1539a122-7f69-49c5-9f1d-273b2276ab19**
Локальная копия макета: `data/_sources/ui_redesign/mockup.html` (в коммит не идёт;
шрифт UthmanicHafs зашит data-URI; известная мелочь: в headless-рендере знак ۟
встаёт в строку, в живых браузерах — над буквой).

---

## 1. Контекст и диагноз

Жалобы пользователей: непривычность, перегруженность, неинтуитивные кнопки.
Функционал НЕ урезаем — меняем видимость по умолчанию и группировку.

Точные болевые места (проверено по коду и скриншотам):
- **Шапка** (`index.html` ~1406–1420): 9 кнопок, 6 — разноцветные эмодзи без
  подписей (🔍 📊 📚 🎧 ⚙ ⛶); ⛶ на деле иммерсив, не fullscreen; 🔍 и ⚙ дублируют
  вкладки рельса.
- **Панелька аята** `.ab-head` (~5828–5839): 7–8 кнопок 11–12px на КАЖДОМ аяте
  (🏷, «🌐 N · 📚 M» `covCountLabel`, ☆, 📝, ▶, 🎬, ru/ar peek); тач-цели ~20px.
  В режиме «Источник» (~5543) набор другой — непоследовательность.
- **Запись источника** `sourceEntryHTML` (~5286): до 6 интерактивных элементов;
  `.warn-badge` и `.warn-inline` — ДВЕ кнопки в один `openWarnPopup`.
- **Режимы** `.seg-modes` (`renderCB` ~4980): 6 иконок, подписи прячутся ≤680px;
  📖/📕 неразличимы, 🗂 не угадывается.
- **Панель 📚** (`renderRP` ~6592): 4 ряда фасет-чипов (`renderFacetBar` ~6265)
  ДО контента; `tItem` — 4 тач-зоны на строку; «глазок» скрытия — в другом месте
  (`openSourceManager` из ⚙).
- **Дубли входов** (умножают сложность): поиск ×2, настройки ×2, диафильм ×3
  (аят/плеер/панель 🎧), управление чтецами ×3, режим источника ×3,
  закладки/заметки ×3.
- Хром на телефоне: шапка 43px + режимы 47px + bottomNav 48px = 138px (~16%).

## 2. Исследование (13 приложений) — главный вывод

Победители (Quran.com, Ayat KSU 4.5★/262K, Ayah 4.8★, iQuran, Golden Quran,
Greentech, Tarteel, tanzil) — «чистая страница текста + вся глубина за одним
жестом». Проигравшие (Muslim Pro, Quran Majeed) — «option overload», «чтение не
ощущается главным делом». Тренд 2026 — Study Mode Quran.com (глубина в фокусе
одного аята, не в ленте). ПРЕДУПРЕЖДЕНИЕ (iQuran, Greentech): редизайн, резко
переносящий привычные входы, порождает волну «стало запутанно» — вводить поэтапно,
старые входы гасить постепенно.

**Принцип-рамка** (мерить каждое решение): кнопка имеет право быть постоянно
видимой, только если ею пользуются в каждом аяте, каждую сессию. Остальное — в
одном жесте, но обязано быть находимым.

## 3. Решения пользователя (зафиксированы)

1. Панелька аята — **мягкий вариант**: номер · ▶ · ☆ · ⋯ + пассивные метки.
2. Режимы — **«Аяты | Мусхаф» как виды чтения**; Хифз/Эрудит → карточки на
   Главной + ☰; «Источник» → вход из 📚 («Карта покрытия»).
3. Сначала **макет**, потом код (макет сделан и опубликован).

Групповой штурм (GPT/Gemini/Grok) разобран; из критики GPT принято 7 пунктов из 8
(вошли в план ниже); отклонено: «Офлайн» отдельным пунктом меню ⋯ (дубль входа,
офлайн остаётся в ⚙ Вид → Данные). Из ревью Grok принято: правила именования
чипа, навигатор без дубля списка сур, desktop-hover и колонки в этап 1 явно,
scope-guard этапа 1; ОТКЛОНЕНО: «★ только индикатор, toggle в листе» — ☆
остаётся кнопкой (частое действие, «оставьте на виду аудио и закладки» из
исследования; заполненная ★ и так работает индикатором — отдельного места не ест).
Если после обкатки будут случайные тапы — перевести в индикатор, решение записано
как fallback.

## 4. Утверждённая спецификация

### Этап 1 — очистка чтения (низкий риск)
1. **Панелька аята**: `номер` (кнопка-адрес) · `▶` · `☆` · `⋯`. Явная ⋯ —
   обнаруживаемый вход (номер тоже открывает лист). Заметка/теги — пассивные
   метки-состояния (✏, 🏷 N), НЕ кнопки. Тач-цели ≥40px. Счётчик «🌐·📚» уходит
   с ленты в лист аята («11 переводов · 21 тафсир»).
2. **Лист аята** (нижний лист, механика как `.warn-pop`/`.rec-pop`): Закладка /
   Заметка / Теги / Диафильм / Записать чтение / Копировать… / peek ru-ar /
   «Источники к аяту →» (нынешний cov-попап). **Desktop: hover-действия у аята —
   в этап 1 явно**, не «потом». Лист обязан работать и в раскладке «Колонки»
   (`.wbw-col`: тап по номеру в первой колонке) — там своей панельки почти нет.
3. **Шапка → 4 кнопки**: ☰ · позиция «Аль-Бакара · 255 ▾» (кнопка-навигатор) ·
   🔍 · ⋯. Меню ⋯: Прогресс, Вид и настройки, Во весь экран, О программе.
   Аудио БЕЗ отдельной кнопки: ▶ у аята запускает плеер, чтец меняется в плеере.
   **Навигатор** = поле «2:255», недавние места и ссылка «Все суры →» (открывает
   вкладку «Суры»), а НЕ второй полный список сур (не дублировать ☰; Grok).
4. **SVG-иконки**: единый монохромный inline-набор (стиль Lucide) вместо эмодзи
   в органах управления (эмодзи остаются в контенте). Обоснование: эмодзи
   рендерятся по-разному на iOS/Android/Windows/Linux.
5. **Предупреждения — один компонент**: цветной индикатор уровня + одна
   кликабельная плашка (бейдж и плашку не дублировать, сигнал не терять).
6. Дедупликация входов: диафильм — лист аята + плеер; чтецы — только плеер/⚙;
   ▲▼ сортировки в 📚 — только в явном режиме «упорядочить»; `.immersive-exit`
   (opacity .3) сделать видимым.
7. **Scope-guard этапа 1** (Grok): трогаем только хром чтения. hrStart, erudit,
   карта хифза, офлайн-менеджер, данные, SYNCED_KEYS — НЕ трогать.

### Этап 2 — структура
8. **Панель чтения** (вторая полоса, auto-hide при прокрутке вниз, шапка
   остаётся): `[ Аяты | Мусхаф ]` + чипы `[Кулиев +1]` (вход в источники,
   показывает включённое) и `[Слои]` (пословный/таджвид/редакции). Чип НЕ в
   шапке — на 360px не влезает (проверено скриншотом). Именование чипа (Grok):
   1 источник → «Кулиев»; 2+ → «Кулиев +N»; только арабский → «Оригинал»;
   длинные имена — truncate, без переноса на 2 строки.
9. **Режимы**: убрать `.seg-modes` из 6; Хифз/Эрудит — Главная + ☰; Источник —
   из 📚 (сам код srcMode остаётся, убирается только вход из бара). Переходный
   период: тосты «Функция теперь здесь».
10. **Панель 📚 → вкладки «Выбрано | Каталог | Оригинал и слои»**; фасеты — за
   кнопкой «Фильтры»; пояснения — за «?»; паспорт — строка «Об источнике»;
   **пресеты наборов**: «Только перевод» / «Чтение с тафсиром» / «Учёба» /
   «Мой набор» (пресет показывает, что включит; пользовательская подборка
   автосохраняется в «Мой набор», молча не перезаписывать).
11. Одноразовая подсказка при первом входе: «Тап по номеру аята или ⋯ — действия».

### Этап 3 — отдельные задачи (НЕ смешивать с редизайном)
- Study Mode (фокус-экран аята: Тафсиры/Слова/Корни/Моё, листание ‹ ›) —
  **дополняет** ленту, тафсиры из ленты НЕ выселяются (ядро продукта — чтение
  нескольких тафсиров подряд).
- Мусхаф: бокс перевода текущего аята под страницей (tanzil-паттерн).
- Плеер: произвольное N повторов + настраиваемая пауза между повторами
  («успеть повторить за чтецом», Ayat KSU).
- Копирование диапазона (арабский/перевод/с адресом), раздельный размер шрифта
  тафсира, клавиатурная навигация (↑↓ аяты, ←→ страницы, пробел — плей),
  проверка памяти на странице мусхафа (виден один аят, тап = подглядеть).
- Полировка: border-radius, transitions .2s, +10–15% line-height тафсиров.
- Пятисекционная навигация (Главная/Читать/Изучать/Практика/Моё) — только
  ПОСЛЕ обкатки этапов 1–2, отдельным решением.

## 5. Реализационные заметки
- Всё живёт в `index.html`; листы делать по механике существующих нижних листов;
  данные, deep-links `#s:a`, SYNCED_KEYS не трогаются.
- Проверка — headless-скриншоты (см. CLAUDE.md), обязательно ширины 360/390/430.
- Старые popап-функции (`openTagPopup`, `openCovPopup`, `mediaRecPopup`,
  `openDiafilm`) переиспользуются из листа аята.
- Обновить `HELP.md` и запись в `CHANGELOG.md` после каждого этапа.
