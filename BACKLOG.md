# Tafsir Library — Backlog

Цель: удобная платформа для чтения тафсиров. Сотни тафсиров/переводов.
Особый акцент на мобильной версии: удобно, красиво, плавно. Лёгкое переключение
тафсиров и чтение со смартфона.

Реализуется по порядку. Каждый пункт — функциональный и тестируемый, приложение
остаётся рабочим после каждого шага.

---

## Phase 1 — Масштабируемая загрузка данных (блокер №1) ✅
- [x] 1.1 Разрезать монолитные `tafsirs/<id>.json` на чанки по сурам:
      `tafsirs/<id>/<surah>.json` = `{ "<ayah>": "text" }`. Скрипт `split.py`.
- [x] 1.2 `loadChunk(id, surah)` — ленивая загрузка только нужной суры + кэш по
      ключу `id|surah`. Back-compat: offline `TL_DATA[id]` и старый монолит.
- [x] 1.3 LRU-выгрузка чанков (ограничить память). MAX_CHUNKS=80.
- [x] 1.4 Обновить `convert.py`, чтобы новые конвертации сразу давали чанки + индекс.

## Phase 2 — Мобильные быстрые победы ✅
- [x] 2.1 Убрать hover-only контролы (теги, сортировка) → `@media(hover:none)` всегда видны.
- [x] 2.2 На узких экранах отключить layout «Колонки» (только вертикальный стек).
- [x] 2.3 Вернуть пинч-зум (убран `user-scalable=no`/`maximum-scale`).
- [x] 2.4 Регулятор размера шрифта чтения (A− / A+), запоминается в localStorage.
- [x] 2.5 Смягчить анти-копирование: убран блок контекстного меню и Ctrl+S/U.

## Phase 3 — Плавный рендеринг ✅
- [x] 3.1 Виртуализация чтения: скелет из всех аятов, тело каждого аята
      гидрируется/очищается по IntersectionObserver (rootMargin 1200px). Проверено:
      Аль-Бакара 286 аятов → в DOM только ~10 тел вместо 286.
- [x] 3.2 Переключение тафсиров стало дешёвым: перестраивается лёгкий скелет +
      гидрируются только видимые аяты; топовый видимый аят сохраняется (scroll anchor).
      Примечание: layout «Колонки» (только десктоп) пока без виртуализации.

## Phase 4 — Роутинг и шеринг ✅
- [x] 4.1 URL-хеш `#сура[:аят]`; goSurah/поиск пишут хеш (pushState) — работает «назад»;
      на мобиле «назад» закрывает открытую панель (popstate).
- [x] 4.2 Deep-link: `index.html#2:255` открывает суру и подсвечивает аят. Проверено.

## Phase 5 — Поиск на масштабе ✅
- [x] 5.1 `build_index.py` строит инвертированный индекс `data/index/<id>.json`
      (слово → "сура:аят ..."). Поиск грузит индекс (не полный текст), находит аяты
      по словам запроса (AND + подстрока по словам), текст для показа берёт из чанков
      только для найденных аятов. Fallback на полный скан, если индекса нет.
      Проверено: «милостивого» → 27 результатов, грузится index/kuliev.json, не монолит.
- [x] Побочно исправлен баг `normalizeArabic`: диапазон `ؐ-ٟ` захватывал
      буквы (0x621–0x64A) и стирал весь арабский текст → арабский поиск был сломан.
      Теперь корректные диапазоны огласовок (в index.html и build_index.py).
- Примечание: «искать во всех» при сотнях тафсиров грузит все индексы — на больших
  объёмах в итоге нужен бэкенд/серверный индекс.

## Доп. — Панель «Тексты и тафсиры»: фасеты + flyout ✅
- [x] Фасет-фильтры в config: `kind` (quran/translation/tafsir), `era`, `tags[]` (+ `facetTags`
      словарь направленностей). Проставлены всем текущим тафсирам.
- [x] Правая панель переписана: чипы-фасеты (Язык/Тип/Эпоха/Направленность, AND между осями),
      закреплённая секция «✓ Включено» с reorder и мета-пилюлями, «Каталог» из ещё-не-включённых,
      фильтруемый поиском + фасетами.
- [x] Панель стала flyout: выезжает справа с затемнением, закрывается кликом по фону / «назад» /
      Esc / ✕ (popstate + navBack, чтобы не оставлять висящие записи истории).
- [x] Арабский текст — расширяемая секция `kind:quran`: «с огласовками» (`_arabic`) и
      «без огласовок» (`_arabic_clean`), можно показывать несколько сразу. 3-й вариант
      «без диакритических знаков» добавляется как ещё одна запись `kind:quran` (следующий этап).

## Phase 6 — Поддерживаемость ✅
- [x] 6.1 Добавлен `escAttr()` для значений в inline-обработчиках (экранирует `'`, `\`,
      `"`, `<`, `>`, `&`). Применён ко всем onclick с путями тегов. Проверено: тег
      `O'Brien "x" <b>` теперь даёт корректный `onclick="selectTag('O\'Brien …')"`.
- [x] 6.2 Дубликат `renderPopupNode` (×2) вынесен в одну функцию `popupNodeHTML`.

## Phase 7 — Тафсир Ибн Касир (рус.) — доработки
Импорт сделан (`build_kathir_ru.py`, суры 1–19 и 59–114) — см. CHANGELOG 2026-06.
Осталось:
- [ ] **Недостающие суры 9–10 и 20–58.** Нужны TXT недостающих томов Мухтасара;
      лучшая версия экстракции — серия файлов `80179…` (формат B: двухколоночный,
      номера аятов, чистый арабский с огласовками). Серия `8865…/8933…/9497…` —
      старая (формат A, битый арабский).
- [ ] **Инлайн-цитаты арабского в комментарии** сейчас вырезаны (держимся на
      русском глоссе «…»). Можно ре-сорсить из `_arabic.json` сопоставлением
      фрагментов — нетривиально.
- [ ] **Сноски формата A**: маркеры распознаются эвристикой (цифра, приклеенная
      к слову) — возможны редкие ложные срабатывания, стоит выборочно проверить.
- [x] **(app) «У аята — все доступные тафсиры»** — реализовано **Системой покрытия**
      (а не отдельной фичей): бейдж `🌐 N · 📚 M` в шапке аята → попап со списком
      источников у аята (✓/—, группы Переводы/Тафсиры), действия «только здесь»
      (разовый инлайн-показ) и «постоянно» (включить всюду). Шире исходной задумки
      (включены и переводы, не только `kind:"tafsir"`).
- [x] **(app) Главная страница** — пятый режим (🏠) наряду с Аяты/Слова/Мусхаф/Источник;
      экран по умолчанию при открытии без deep-link (хеш `#home`). Блок «Продолжить
      чтение» (из `lastPos`) + «Недавнее» (лента `recent`, MRU, до 5 сур, по одной на
      суру) + настраиваемые быстрые ссылки. Синтаксис: `N` (вся сура) / `N/A` (аят) /
      `N/A-B` (диапазон), через запятую/строку, + опц. своя подпись; подсказка в поле.
      Дефолтный список из распространённых сур. **Пятница** (`getDay()===5`) →
      Аль-Кахф выделенной плиткой первым. Хранилище: `tl_recent`, `tl_quickLinks`.
- [x] **(app) Экспорт/импорт личных данных** — один `.json` (заметки + закладки +
      теги + дерево тегов) для бэкапа/переноса. Кнопки в панели 📚 под «Мои заметки».
      Импорт = слияние: новые добавляются, теги объединяются, дерево мёрджится по
      именам; при конфликте заметок — диалог (Оставить мои / Дописать обе / Заменить).

## Статусы проверки — два уровня (UX)
- [x] **Разделить предупреждение `caution` на два уровня.** Сейчас `caution:true`
      стоит на ВСЕХ русских источниках (Кулиев, Крачковский, Саади, Абу Адель и
      т.д. — «проверен только Багави Исмаилова»), но текст паспорта писался под
      маргинальные переводы: «возможны… вероучительные отклонения… сверяйтесь с
      авторитетными переводами». На признанных переводах (Кулиев) совет «сверяйтесь
      с авторитетными переводами» звучит странно. Ввести два статуса:
      - **«не выверено лично мной»** (мягко) — для признанных переводов/тафсиров;
      - **«относитесь с осторожностью»** (жёстко, текущий текст) — для маргинальных.
      Реализация: напр. поле `reviewLevel: "unverified" | "caution"` (или отдельный
      флаг), развести текст-предупреждение и пилюли в `aboutStatus`/`tafsirMeta`.
      Одобренное (`approved` / сегмент) — как есть (зелёная заливка + `✓✓`).

## 🎓 «Эрудит» — занимательные упражнения (модуль `erudit.js`)
Цель модуля: прививать знание Корана через короткие упражнения. Движок раунда
общий; каждое новое упражнение — запись в `EXERCISES` с `genQ(cfg,pool)`.

- [x] **Каркас модуля + упражнение «Названия сур»** (2026-08-03): режим 🎓,
      ленивый ES-модуль, охваты/длина/виды заданий, взвешенный пул, разбор ошибок,
      статистика `tl_erudit`. Виды: номер↔имя, араб.↔рус., число аятов, соседи и
      расстановка сур по порядку.
- [x] **Упражнение «Аяты»** (2026-08-03): из какой суры · продолжить аят ·
      следующий аят · пропущенное слово; арабский/перевод/вперемешку.
      Отбор аятов решён предрасчётом `build_erudit_pool.py` (169 межсурных
      двойников) + правилами (короче 4 слов, последний аят суры, басмала).
- [ ] **Упражнение «Аят по теме»** — данные `data/topics` (фихрист, 627 тем с
      аятами) уже в репо: «к какой теме относится этот аят» / «выбери аят по теме».
- [ ] **Тартиб: расставить аяты по порядку** — разбросанные аяты одной суры (или
      слова одного аята) вернуть на места. Полезно для хифза; переиспользовать
      механику `type:"sort"` из «Названий сур».
- [ ] **«Лестница знания»** (формат «кто хочет стать…», но название без игрового
      оттенка) — 15 ступеней возрастающей сложности, подсказки (50:50, «спросить
      у книги» — показать тафсир, замена вопроса).
      **Нужен банк вопросов:** `data/quiz/*.json`, класс `quiz` в `SOURCES`
      (чтобы при росте переехать на R2 одной строкой). Формат записи:
      `{id, level 1..5, topic (ulum|tafsir|sira|surah|ahkam), q, opts[], a, why, ref, reviewed}`.
      **ВАЖНО:** вопросы по тафсиру/фикху, сгенерированные ИИ, показывать ТОЛЬКО
      с `reviewed:true` — после вычитки мейнтейнера (как очередь ревизии
      ИИ-тафсиров). Вопрос без далиля/с спорным ответом в банк не идёт.
- [ ] **Мелочи по каркасу:** статистика по видам заданий (а не только по
      упражнению), «повторить только ошибки» кнопкой с экрана итога, ежедневная
      разминка (5 заданий из слабых мест).

## Похудение репозитория — вынести тяжёлые не-R2 источники на R2
- [ ] **Перенести крупные in-repo источники на R2** (как уже сделано для kathir_ru,
      baghawi, baghawi_ar, qurtubi_*, mawsua, асары). Кандидаты по весу отслеживаемого
      гита (~213 МБ сейчас): `saadi_ru` (11 МБ json + 10 МБ `.js`-зеркало),
      `ibn_abi_hatim` (11 МБ), `muntahab` (3 МБ), `abuadel` (2.8 МБ) и их индексы.
      Схема штатная: `dataBase`→R2 в config, данные в `r2-data/`, `.gitignore`,
      `./upload_r2.sh`, пересобрать fill/coverage. Итог: репо/сайт GitHub Pages
      похудеет, билды Pages ускорятся. Не срочно (сейчас 213 МБ < лимита 1 ГБ).
      NB: `.js`-offline-зеркала тяжёлых источников тоже тянут вес — пересмотреть,
      нужны ли они (offline `file://`) для R2-источников (их всё равно не открыть
      офлайн без сети к R2).

## Слова — не отдельный режим, а СЛОЙ поверх ленты ✅ (сделано 2026-07-17)
- [x] **Галочка «Со словами» вместо режима 🔤.** Сетка слов встаёт вместо арабской
      строки внутри блока аята, переводы/тафсиры остаются под ней; галочка — в 📚
      «Оригинал» рядом с таджвидом (слой над поверхностью, а не редакция). Режим 🔤
      удалён (`renderWbw`), в переключателе 5 режимов, `wbwMode` → `wbwLayer`.
- [x] **Начертания в слое.** Печатное → глиф пака, текстовое → склейка сегментов
      `data/wbw/morph`; сходимость слов пак↔морфология сверена на всех 6236 аятах.
      Концевой знак в слое не рисуется (у блока есть номер).
- [ ] **Таджвид на словах в текстовых начертаниях** (осталось). Спаны
      `data/tajweed` индексированы по кодпоинтам `_arabic`, а не по словам корпуса
      (в 2:255 у корпуса 50 слов против 58 при разбиении `_arabic` по пробелам) —
      чтобы красить слова, нужен перенос индексов на границы слов корпуса (та же
      техника, что в `build_tajweed.py`: difflib-перенос). Сейчас в слое цвета
      доступны только в печатном начертании (цвет в самом шрифте), в панели о
      ограничении сказано.

> Пересборка источника после правок: `python3 build_kathir_ru.py && python3 compute_fill.py && python3 sync_config.py`
</content>
