# CLAUDE.md — ориентир для ассистента

Памятка для нового сеанса Claude. Подробности — в `GUIDE.md` (мейнтейнер),
`HELP.md` (пользователь), `CHANGELOG.md` (история), `BACKLOG.md` (планы).

---

## Что это
**Библиотека тафсиров** — статический веб-сайт для чтения Корана, переводов и
тафсиров с телефона и десктопа. Без сборки и фреймворков: один `index.html`
(ванильный JS) + данные в `data/` (JSON). Хостится как статика (GitHub Pages).

## Как запустить / проверить
- Локально: `python3 -m http.server 8000` в корне → http://localhost:8000/
  (открывать `file://` нельзя — браузер блокирует загрузку JSON; для offline есть
  `.js`-зеркала, см. ниже).
- Тестов нет; проверка — глазами в браузере. Питон-скрипты идемпотентны.

## Стек и структура
```
index.html          ← всё приложение (UI + рендер + поиск), ванильный JS
media.js            ← медиа-модуль (аудио-плеер): ES-модуль, ленивый import()
                      из index.html; чтецы — config.json → audio.reciters
data/
  config.json       ← ЕДИНЫЙ ИСТОЧНИК ПРАВДЫ: список источников и групп
  config.js         ← offline-зеркало (генерится sync_config.py — НЕ править руками)
  tafsirs/
    <id>.json       ← весь источник {сура:{аят:текст}} (для поиска)
    <id>/<surah>.json ← чанк по суре {аят:текст} (ленивая загрузка loadChunk)
    <id>/index.json ← список заполненных сур
    _arabic.json / _arabic_clean.json / _arabic_rasm.json ← канонический Коран
  index/<id>.json   ← инвертированный индекс поиска (build_index.py)
  qpc/v1/           ← данные печатного мусхафа (build_qpc.py)
  qpc/v4t/          ← данные цветного таджвид-мусхафа QPC v4 (build_qpc4t.py);
                      шрифты пака — НА R2 (r2-data/fonts/qpc-v4-tajweed, gitignored)
  wbw/              ← пословный перевод + морфология (build_morph.py)
    roots.json      ← конкорданс: корень → {n, глоссы, формы+адреса} (build_concordance.py);
                      ОДИН файл на весь Коран (карта обратная), грузится лениво
  about/<id>.md     ← вступление «о переводе» (кнопка ℹ); about/app.md = экран «О программе»
  img/<id>/         ← скан-источники kind:"image": КАРТЫ страниц (<sura>.json,
                      _about.json) ЛЕЖАТ ЗДЕСЬ; сами webp-страницы — на R2 (gitignored)
  _sources/         ← СЫРЬЁ сборок (gitignored! пересоздаётся скриптами)
fonts/              ← самохостимые шрифты (.woff2)
```

## Расположение данных (абстракция источников)
- **Реестр `SOURCES`** в начале `<script>` в `index.html` — для каждого КЛАССА
  ресурса `{baseUrl, pathTemplate}`. Классы: `config`, `coverage`, `warnings`
  (предупреждения об ошибках в источниках), `quran_text`,
  `tafsir_text`, `tafsir_monolith`, `search_index`, `glosses`, `morphology`,
  `about`, `concordance` (индекс корней), `surah_meta` (классификации сур для оверлеев карты хифза),
  `mushaf_glyphs`, `mushaf_fonts`, `mushaf_fonts_r2` (шрифты таджвид-пака на R2),
  **`mushaf_scans`** (webp-страницы скана),
  **`scan_maps`** (карты-списки страниц скана), **`audio_cdn`** (поаятное CDN-аудио
  чтецов для media.js: `{subdir}/{file}`, файл `NNNSSS.mp3` — паддинг у вызывающего),
  `audio` (задел), `arabic_volumes` (задел).
- **Все URL к данным — только через `resolveUrl(class, params)`.** Прямых/«зашитых»
  путей в коде нет. Чтобы перенести класс на другое хранилище, меняется ТОЛЬКО
  `baseUrl` нужного класса в `SOURCES` — вызывающий код не трогается; разные классы
  могут жить на разных базах.
- **Переопределение базы на уровне ИСТОЧНИКА**: поле `dataBase` в записи источника
  в `config.json`. `resolveUrl` применяет его для по-аятных классов источника
  (`tafsir_text`, `tafsir_monolith`, `search_index`, `quran_text`); лёгкие общие
  классы (config/coverage/about/…) остаются локальными. Так тяжёлый источник можно
  держать целиком на R2 (монолит+чанки+индекс), а остальные — в репо. Пример —
  `mawsua_masur` (свод бил-ма'сур, ~120 МБ): данные на R2, в репо НЕ коммитятся
  (`.gitignore`), пересборка — `build_mawsua_masur.py` → split/index/fill/coverage.
- Текущие базы: почти всё `data`; `mushaf_fonts="fonts"`; **`mushaf_scans` →
  Cloudflare R2** (`https://pub-d99a56b61d914461b5864f421a013097.r2.dev`) — см.
  «Сканы на R2» ниже.

## Модель данных (главное)
- Каждый тафсир/перевод/словарь = `{ "<номер_аята>": "markdown-текст" }` по сурам.
- **`config.json` — единственное место правки** метаданных источника. После любой
  правки запусти **`python3 sync_config.py`** → пересоберёт `config.js`.
- Поле `fill` (доля заполненных аятов) **не править руками** —
  **`python3 compute_fill.py`** пересчитывает по данным.
- Поля источника: `id, file, name, nameAr, author, group, kind(quran|translation|
  tafsir|image), lang(ru|ar), era, tags[], year, fill`. Опционально: `about, caution,
  approved, translator, source…` (см. GUIDE §2).
- Группы (`config.json.groups`): quran / translations / russian / modern_ar / classical_ar.

## Разметка текста тафсиров (подмножество Markdown, рендер в index.html)
- `**жирный**`, `__жирный__`, `*курсив*`, `` `моно` ``, `# … ####`, `> цитата`,
  списки, таблицы, `---`.
- **ВАЖНО:** одиночное `_подчёркивание_` НЕ курсив (только `*звёздочки*`).
- Сноски: `текст[^1]` + отдельной строкой `[^1]: примечание` → список под аятом.
- Карточки: `[[видимое::текст]]` → кликабельное слово с попапом (грамматика).
- Текст HTML-экранируется (произвольный HTML вставить нельзя).

## Скрипты сборки данных
| Скрипт | Назначение |
|---|---|
| `sync_config.py` | `config.json` → `config.js` (запускать после правки конфига) |
| `compute_fill.py` | пересчёт `fill` у всех источников |
| `build_coverage.py` | индекс покрытия `data/coverage.json` (какие аяты есть у источника) |
| `convert.py` | текстовый файл → чанки JSON + индекс (см. GUIDE §5) |
| `split.py` | монолит `tafsirs/<id>.json` → чанки по сурам |
| `build_index.py` | инвертированный индекс поиска `data/index/<id>.json` |
| `build_qpc.py` | глифы печатного мусхафа (QPC v1) |
| `build_qpc4t.py` | глифы цветного таджвид-мусхафа (QPC v4, скрейп превью QUL; сырьё в `_sources`) |
| `build_tajweed.py` | спаны правил таджвида `data/tajweed/` из cpfair (перенос индексов difflib) |
| `build_morph.py` | пословный перевод + морфология (`data/wbw`) |
| `build_concordance.py` | конкорданс: `data/wbw/roots.json` (корень → все вхождения) из `data/wbw/morph`; сети не требует, гонять после `build_morph.py` |
| `build_alyautdinov.py` | импорт «Богословского перевода» Аляутдинова (4 тома cp1251 → монолит; блоки `N:M–K`, сноски `[N - …]`, мукаддима) |
| `build_topics.py` | тематический указатель (фихрист): `data/topics/` из онтологии Quranic Arabic Corpus (SQLite в `_sources`), перевод названий — `names_ru.json` |
| `build_kathir_ru.py` | импорт русского Ибн Касира (см. ниже) |
| `fix_kathir_blocks.py` | дозаполнить блочные отсылки kathir_ru (формат A) — разово |
| `validate_data.py` | проверка целостности данных (запускать перед пушем) |

## Текущее состояние и подсистемы (что уже есть в коде)
Подробная история — в `CHANGELOG.md` («новое сверху»). Кратко, последнее сверху:
- **Хифз: охват «несколько сур»** (2026-07-17): `HF.scope` получил ТИП —
  `{kind:"juz",juz,surah,from,to}` (как было) либо `{kind:"suras",list:"78-114, 36"}`.
  Раньше охват был жёстко привязан к джузу, и муфассаль/«Бакара+Имран» не выражались
  вовсе. Разбор строки — `hifzParseSuras` (диапазоны `78-114`, перевёрнутые тоже,
  разделители `, ; пробел`; плохие токены ВОЗВРАЩАЮТСЯ и показываются в UI, а не
  глотаются). Раскрытие охвата — по-прежнему единственная точка `hifzScopeSuras()`,
  поэтому пул/статистика/цепочки/слабые места не трогались. Миграция: сохранённый
  охват без `kind` → `"juz"`. `hifzQuizSura` (🎯 в строке суры) теперь ставит
  `{kind:"suras",list:"<s>"}` — сура берётся целиком, даже если вне текущего джуза.
- **Поиск и Заметки — вкладки рельса** (2026-07-17): 🔍 больше НЕ плавающая панель
  (`.search-panel`, `searchOpen`, `#spInput` удалены) — он вкладка (`renderSearchPanel`),
  строка ввода = общее поле шапки (`#sbSearchInp` → `onSearch` → `scheduleSearch`,
  панель на вводе НЕ перерисовывается: `doSearch` точечно пишет в `#spResults`).
  `doSearch` читает `ST.search`/`ST.searchAll` (не DOM). Кнопка 🔍 и Ctrl+F зовут
  `openSearchTab` (как ⚙ открывает вкладку Вид). 📝 Заметки — новая вкладка
  (`renderNotesPanel` + экран `renderNotesScreen`/`ST.notesView`, `noteKeysSorted`,
  `noteSnippet` снимает markdown): раньше списка заметок НЕ БЫЛО вовсе — только
  счётчик в подвале и поиск по тексту. Попутно: `.sp-count.hidden` не имел правила
  (класс вешался, счётчик оставался виден).
- **Слой «Со словами»** (2026-07-17): пословный разбор перестал быть отдельным
  режимом 🔤 и стал СЛОЕМ ленты — галочка в 📚 «Оригинал» рядом с таджвидом
  (`ST.wbwLayer`, ss-ключ `wbwLayer`, миграция со старого `wbwMode`). Сетка слов
  встаёт вместо арабской строки ВНУТРИ блока аята (`buildAyahBody`), переводы и
  тафсиры остаются под ней (главный выигрыш: разбор слова при чтении тафсира).
  Работает в обеих раскладках («Список» и «Колонки», `.wbw-col`). Слово рисуется
  двумя путями (`wbwGridHTML`): печатное начертание → глиф пака (`activePack()`),
  текстовое → склейка сегментов `data/wbw/morph` (`.wbw-ar-text`, начертания
  ⚙ Вид). Слова паков и морфологии сходятся 1:1 — сверено на всех 6236 аятах.
  Данные слоя — `loadWbwLayer` (глоссы + морфология|глифы). Концевой знак аята в
  слое не рисуется (у блока есть номер; в паке это глиф, у текстовых шрифтов его
  нет). Атрибуция глосса/разбора (`wbwAttrHTML`) — под лентой, пока слой включён
  (требование лицензий data-quran CC BY-NC-ND / корпус GPL). Таджвид на словах —
  только в печатном (цвет в шрифте): спаны `data/tajweed` привязаны к кодпоинтам
  `_arabic`, а не к словам корпуса. `renderWbw` удалён.
- **Конкорданс — корни слов** (2026-07-17): «где ещё встречается это слово». Данные —
  `data/wbw/roots.json` (`build_concordance.py` из своей же `data/wbw/morph`, сеть не
  нужна; 1651 корень / 50 268 вхождений / 17 867 форм, ~1 МБ, класс `concordance`,
  ленивая загрузка `loadRoots`, глобал `ROOTS`). Корню дана подпись — до 4 частых
  русских глоссов (`data/wbw/words`, покрытие 100%), по ним же корень ищется
  по-русски. Указатель — ВКЛАДКА панели (`renderRootsPanel`, поиск через общее
  `ST.search`, отбор `rootsMatching` общий с полноэкранным) + полноэкранный вид
  («⛶ На весь экран») — как у закладок; ссылка с Главной → `openRootsPanel`. ДВА экрана на ОДНОМ поле `ST.rootView`: `"*"` — указатель
  (`renderRootsIndex`, поиск `rootsFilter` перерисовывает только `#rootsList`, иначе
  теряется фокус), `"<корень>"` — вхождения (`renderRootOccurrences`: чипы-формы
  `ST.rootForm`, порции по 20 `ST.rootLimit`, текст тянется ТОЛЬКО для показанной
  порции). Три входа: ссылка «🌿 Корни слов» на Главной, тап по корню в грам-карточке
  (`.mcard-root`), «‹ Все корни». `ST.rootView` гасится везде, где `topicSel`.
  Заодно: `setMode` теперь сбрасывает `topicSel`/`selectedTag`/`rootView` — раньше с
  экрана тем/тегов тап по «📖 Аяты» не уводил никуда.
- **Предупреждения об ошибках в источниках** (2026-07-09): редакторский слой,
  текст источника НЕ трогает. Данные — `data/warnings.json` (класс `warnings`,
  грузится в `loadWarnings` как coverage; глобал `WARN`):
  `{ "<id>": { "<с>:<а>": [{level,title,text}], "*": [...] } }`. Три уровня
  (`WARN_LEVELS`): `critical`/`moderate`/`minor` (дефолт moderate). Рендер — в
  `sourceEntryHTML` (`warnFor`/`warnTop`): значок `.warn-badge` + рамка
  `.ab-entry.warn-<lvl>`; тап → `openWarnPopup` (нижний лист `.warn-pop`,
  `mdRender`). Привязка к аяту и ко всему источнику (`"*"`). Демо-записи — на
  `krachkovsky` (выкл. по умолчанию).
- **Медиа-модуль, фазы 1–2: аудио-плеер + записи** (2026-07-08): `media.js` —
  ES-модуль, ленивый `import()` из index.html (`mediaMod()`, `MEDIA_VER` —
  кеш-бастер, поднимать при правке media.js). Ось аята: указатель `{sura,ayah}`
  + событие смены (экспорт `onAyahChange` — хук для караоке/визуального слоя);
  текст следует за указателем (подсветка `.media-cur`, скролл; в мусхафе —
  перелистывание). Чтецы — `config.json → audio.reciters` (массив; default —
  аш-Шатри/EveryAyah), класс `audio_cdn` в SOURCES. Записи пользователя: 🎙 у
  аята → попап (запись/прослушать/удалить, `MediaRecorder`, blob как есть +
  MIME), хранение ТОЛЬКО в IndexedDB `tafsirMedia` (store `recordings`, ключ
  `[set,"s:a"]`); наборы — источники `rec:<id>` (тип `audio_user_recording`,
  скип незаписанных аятов), метаданные наборов — `tl_mediaSets` (localStorage).
  UI: ▶/🎙 у номера аята, блок «🎧 Аудио» в 📚 (`audioBlockHTML`),
  панель `#mediaPill.media-bar` — НЕ плавает, вставлена над `#bottomNav`,
  кнопки сгруппированы (`.mb-grp`) и переносятся (`flex-wrap` + media-query
  ≤430px), видна и в иммерсиве; выбор источника из плеера (`.media-menu`,
  кнопка `data-act="src"`), повтор аята ×2/×3/∞, диапазон A–B, скорость,
  сворачивание ⌄/⌃ → `tl_mediaMin`. Попап записи (`.rec-pop`) — нижний лист,
  аят подкручен выше (виден при записи), рисуется синхронно (индикатор из
  `RECKEYS`, idb догоняет). Настройки `tl_mediaReciter`/`tl_mediaRate`/
  `tl_mediaSets`/`tl_mediaRecSet` — НЕ в SYNCED_KEYS (записи локальны). Повтор
  выбирается из меню (`openRepeatMenu`, кнопка `data-act="loop"`): аят ×2/×3/∞
  или диапазон A–B (начать→закончить). **Фаза 3 — визуальный слой**: полоса
  `#visualBand` (сосед `#centerBody`, `visual-band`, 2 слоя crossfade+Ken Burns)
  над лентой чтения; пул = картинки тем аята из `data/topics/byAyah.json` +
  `data/media/visual.json` (`topics`/`pins`/`default`); выбор при входе в аят,
  держится в пределах аята, не повторяет предыдущую; классы `visual_map`/
  `visual_media` в SOURCES; `render_type:image` (SVG-заглушки `data/media/img/`);
  включается галочкой «🖼» (`tl_mediaVisual`), независимо от аудио, без аудио
  ведётся скроллом (верхний видимый аят), хук `afterRender` из `renderCenter`.
  Отложены: Cache API-докачка офлайна, тайминги QUL, караоке, видео, свои
  картинки пользователя.
- **Богословский перевод Аляутдинова** (2026-07-08): источник `alyautdinov`
  (группа «Переводы», 114 сур, `build_alyautdinov.py`) с флагом `reviewLevel:
  "caution"` («не проверено»). Имя ведёт словом «Аляутдинов», чтобы не путать с
  «Богуславский (скан)». Тогда же: `--sb` (размер боковой панели) распространён
  на теги/темы/закладки (был только для сур и источников); регэксп сносок в
  `mdRender` отбивает `[^n]:` и с ведущим отступом.
- **Тематический указатель (фихрист)** (2026-07-08): read-only слой «📑 Темы»
  (5-я вкладка боковой панели) — дерево коранических тем (3 раздела / 695 тем /
  627 с аятами) из онтологии Quranic Arabic Corpus (© Kais Dukes, GPL; SQLite →
  `build_topics.py` → `data/topics/{tree,verses,byAyah,meta}.json`; названия
  переведены на русский, `names_ru.json`). Тап по теме → её аяты (`renderTopicResults`,
  переиспользует механику `renderTagResults`); кнопка «➕ в мои теги» копирует тему
  в личные `tagTree`/`ayahTags` (корень «Тематический указатель»). Класс `topics`
  в `SOURCES`; состояние `ST.topicSel` (сбрасывается везде, где `selectedTag`).
  Личные теги пользователя НЕ трогаются. byAyah.json (обратная карта) собран для
  будущих бейджей тем у аята — пока не подключён.
- **Таджвид фаза B** (2026-07-05): галочка «Цвета таджвида» работает в любом
  начертании — флаг `ST.tajweed` (отделён от `arFont`, миграция qpc4t→флаг):
  печатный → пак qpc4t, текстовые шрифты → спаны `data/tajweed/<s>.json`
  (`build_tajweed.py` из cpfair; класс `tajweed_spans`, `tajweedHTML`,
  `tajweedActive`). Тап по букве → карточка правила (`TJ_RULES`/`showTjCard`),
  «легенда» — `showTjLegend`. Палитра `--tj-*` следует за поверхностью чтения
  (тема/скины). `pagePack()` учитывает флаг (Мусхаф).
- **Разводка панелей** (2026-07-05): редакции `_arabic*` и галочка «Цвета
  таджвида» — в блоке «Оригинал» панели источников 📚 (`quranBlockHTML`;
  галочка сама включает печатное начертание, `toggleTajweed` qpc1↔qpc4t);
  шрифты — в ⚙ Вид (`AR_UI_FONTS`, «Печатный» покрывает qpc1+qpc4t через
  `isPrintedFont`/`setArFontUI`, отдельной кнопки «Таджвид» нет).
  Принцип: 📚 = что читать, ⚙ Вид = как выглядит.
- **Таджвид-мусхаф** (2026-07-05): второй пак `qpc4t` в `PACKS` — цветной QPC v4
  (KFGQPC 1441 г.х.), начертание «Таджвид» в ⚙ Вид, работает поаятно/пословно/
  постранично. Цвета — в COLR-палитрах шрифтов (тёмная тема — CPAL-палитра 1
  через `font-palette`, без подложки). `qpcMeta` ПОПАКОВАЯ (раскладки v1/v4
  расходятся на 25 страницах серединами сур); шрифты на R2
  (`./upload_r2.sh fonts`), данные — `build_qpc4t.py`. Семантики правил нет —
  это фаза B (раскраска текста по данным cpfair, отдельная настройка).
- **Единая система режимов/панелей** (2026-07-04, с 2026-07-17 — 5 режимов):
  (🏠 Главная / 📖 Аяты / 📕 Мусхаф / 🗂 Источник / 🧠 Хифз; «Слова» стали слоем),
  переключатель — иконки (`.seg-modes`, подписи прячутся на узких экранах).
  Боковая панель — вкладки ВЕРТИКАЛЬНЫМ РЕЛЬСОМ слева (`.sb-rail`, с 2026-07-17;
  горизонтальный бар жался с каждой новой вкладкой, у рельса запас по вертикали):
  Поиск / Суры / Теги / Темы / Корни / Закладки / Заметки / **⚙ Вид** (настройки
  живут во вкладке, `viewPanelHTML`; там же «Раскладка переводов» = бывшая
  кнопка Колонки/Список). Правило: панель — навигатор, главное окно — контент;
  закладки открываются и полноэкранно (`ST.bmView`). ВАЖНО: экраны в
  `renderCenter` проверяются по порядку — новый экран обязан сбрасывать
  конкурирующие флаги в goSurah/goAyah/selectTag/setMode/applyHash.
- **Модуль «Проверка заучивания» (хифз)** — слой LEARNING в store `Progress`
  (`hifz.ayahs{declared,verified,srs,log}` + junctions; `getHifzStatus` реальный,
  красит карту покрытия). Режим `ST.hifzMode`: обзорная карта с 3 уровнями зума
  (джузы→суры→аяты), декларации сурой/блоком, взвешенный пул, цепочки K аятов,
  SRS (упрощённый SM-2, «закреплено» = интервал ≥21 дн.). Оверлей-фильтры
  структуры мусхафа и порядки сур — из `data/surah-classifications.json`
  (класс `surah_meta`). Проверка НЕ кормит серию/цель (явный флаг).
- **Модуль «Скорость чтения»** — слой SPEED в store (`speed.runs`): явные
  старт/финиш по странице мусхафа (пилюля `#spdPill`), WPM из пословного слоя,
  личный рекорд страницы + недельный тренд; пассивный тик молчит во время
  замера, финиш отдаёт секунды+аяты одним `recordActivity`. Вход — ⚡ с экрана 📊.
- **Таблица Корана на Главной** (`quranTableHTML`, состояние `QT`): 114 сур /
  30 джузов, порядки (мусхаф/ниспослание/по длине) и фильтры структуры — общая
  механика с картой хифза через `_ovlEnv()` (без слоя заучивания).
- **Сканы Богуславского — на Cloudflare R2** (см. след. раздел). webp убраны из
  репо и из истории git; карты страниц остались локально.
- **Экран «О программе»** — кнопка ℹ в шапке (в любом режиме). Текст —
  `data/about/app.md` (через класс `about` + `mdRender`), правится без кода;
  контакт-канал ссылкой. Markdown-ссылки `[текст](url)` рендерятся (новая вкладка).
- **Абстракция расположения данных** — реестр `SOURCES` + `resolveUrl` (см. выше).
- **Система ПОКРЫТИЯ** источников: индекс `data/coverage.json` (`build_coverage.py`);
  счётчик у аята «🌐 переводы · 📚 тафсиры»; попап со списком источников у аята
  («только здесь» / «постоянно»); **4-й режим «Источник»** (вкладка рядом с
  Аяты/Мусхаф) — экран выбора = компактная карта покрытия, чтение по покрытию,
  переключатель разрывов (серым/схлопнуть/скрыть), навигация по покрытым аятам.
  Скан-источники (kind:image) в покрытие НЕ входят (нет по-аятного покрытия).
- **Главная** (🏠, экран по умолчанию при открытии без deep-link; хеш `#home`):
  «Продолжить чтение» (`lastPos`) + «Недавнее» (`tl_recent`) + настраиваемые быстрые
  ссылки (`tl_quickLinks`, синтаксис `N` / `N/A` / `N/A-B` + опц. подпись); в пятницу
  Аль-Кахф первым. `ST.homeMode`, `renderHome`.
- **5 режимов**: Главная / Аяты / Мусхаф (постраничный QPC, 604 стр.) / Источник /
  Хифз; пословный разбор — СЛОЙ в ленте, не режим. Плюс: заметки
  (инлайн + полноэкранный редактор; подключаются как слой-«источник» `_notes`),
  иерархические теги, закладки (панель + полноэкранный список), поиск
  (инвертированный индекс), бесконтактный/иммерсивный режимы, скан-читалка,
  ⚙ Вид — вкладка боковой панели (шрифты/размеры/темы/раскладка), экран
  📊 Прогресс (цель/серия/карта покрытия) с входами в 🧠 и ⚡.

## Сканы на R2 (kind:"image")
- **webp-страницы** скан-источника — на Cloudflare R2:
  `https://pub-d99a56b61d914461b5864f421a013097.r2.dev/img/<id>/pNNN.webp`
  (класс `mushaf_scans`). В репозитории их НЕТ (gitignore `data/img/**/*.webp`),
  и они вычищены из всей истории git (`git filter-repo`).
- **Карты страниц** (`data/img/<id>/<sura>.json` = `{text:[...],notes:[...]}` и
  `_about.json` = `{images:[...]}`) — ЛЕЖАТ ЛОКАЛЬНО в репо (класс `scan_maps`).
  Это источник истины имён страниц: **нумерация с пропусками** (напр. p015 нет) —
  приложение берёт имена ТОЛЬКО из карт, не предполагает сплошную нумерацию.
  Опционально `ayahNotes` = `{<аят>: markdown}` — текстовые по-аятные примечания
  (у скана нет по-аятного текста; рендерятся баннером `.scan-notice` над страницами).
- Добавить новые страницы: залить webp на R2 в `img/<id>/`, обновить карты в
  `data/img/<id>/`. Менять хранилище — только `SOURCES.mushaf_scans.baseUrl`.
- Бэкап исходной (догрязной) истории был сделан на машине миграции в
  `…/projects/tafsir-app-backup-2026-06-25` (локально, не в репо).

## Проверка headless (нет node/playwright, есть google-chrome-stable)
Сервер: `python3 -m http.server 8000`. Снять DOM/скриншот:
`google-chrome-stable --headless --no-sandbox --disable-gpu --hide-scrollbars
--window-size=440,950 --user-data-dir=<свежий> --virtual-time-budget=12000
--run-all-compositor-stages-before-draw --dump-dom|--screenshot=out.png URL`.
- Состояние через URL: `#сура` (хэш) и `?param` (query) — **query идёт ПЕРЕД хэшем**:
  `?x=1#103`, не `#103?x=1` (иначе попадёт во фрагмент).
- Для экранов, требующих действия (грам-карточка, попап, поиск, редактор, режим
  источника) — временно вставить авто-вызов в конце `loadConfig` (`setTimeout(...)`),
  снять, и ОТКАТИТЬ файл (md5/бэкап). Безопаснее — временная КОПИЯ страницы
  (`_test.html` в корне, в коммит не идёт): index.html тогда вообще не трогается.
- Пара граблей: `window.innerWidth` врёт (clamp ~500) — адаптив проверять через
  `matchMedia`; динамический IntersectionObserver после JS-ре-рендера под
  virtual-time часто не срабатывает — для виртуализации грузить целевую суру
  естественной загрузкой; `let`-глобалы (ST, COV) не висят на `window`, а
  function-declaration (resolveUrl, doSearch) — доступны.
- JS-синтаксис без браузера: извлечь большой `<script>` и `node --check`.

## Деплой и проверка прода
- Repo `git@github.com:ismailovvism-oss/tafsir-app.git`, ветка `main`. Push →
  **GitHub Pages** автосборка → **https://ismailovvism-oss.github.io/tafsir-app/**
  (пересборка ~1–3 мин; нужен корневой `.nojekyll`).
- После пуша опросить живой `index.html` на маркер (с `?cb=$RANDOM` против CDN-кеша),
  убедиться что нужная строка появилась.

## Справка: как собран Тафсир Ибн Касир (рус.) — `kathir_ru` (готов, в данных)
- Источник: русский **Мухтасар** Ибн Касира (пер. Вахитова). Покрытие: суры
  **1–19 и 59–114** (~50%). Сборка: `python3 build_kathir_ru.py`.
- Исходные TXT — **вне репо** (Obsidian-вольт пользователя,
  `…/Тафсир/Ибн Касир/Перевод Ибн Касира/1/`). Промежуточные `.md` —
  в `data/_sources/kathir_ru/` (gitignored). Результат — `data/tafsirs/kathir_ru/`.
- **Две версии PDF-экстракции** (важно для понимания кода): формат **A** (старая,
  суры 1–8, центрированная проза, арабский битый → вырезан) и формат **B** (новая,
  файлы `80179…`, двухколоночная с номерами аятов, арабский чистый).
- Подробности и что осталось — **CHANGELOG.md (2026-06)** и **BACKLOG.md (Phase 7)**.
- Полная пересборка после правок:
  `python3 build_kathir_ru.py && python3 compute_fill.py && python3 sync_config.py`.

## Проверка источника на вероучительные отклонения (предупреждения)
Редакторский слой `data/warnings.json` (текст источника НЕ меняется): значок ⚠ + рамка
у аята, кнопка «⚠ Ответ на заблуждение» + плашка `.warn-inline`, свод в паспорте ℹ
(`aboutWarnings`) + краткая заметка `editorNote` в `config.json`. Уровни: `critical`
🔴 (искажение хукма/акыды), `moderate` 🟡 (смысловой сдвиг/послабление), `minor` 🔵
(тональность). Зелёный НЕ используется (он — «одобрено ✓✓»).
**Методика проверки и переиспользуемый список маркеров/флагов — в `GUIDE.md §9`.**
Кратко: понять «отпечаток» источника → скан по маркерам всего корпуса → триаж
(маркер ≠ проблема) → чтение кандидатов в полном контексте (параллельные субагенты
по темам) → калибровка цвета. Ассистент НАХОДИТ и выдаёт кандидатов (адрес+цитата+
приём+цвет), богословский текст пишет мейнтейнер; такфир не выносим. Ловушки: свёрнутый
повтор в изоляции безобиден на вид; следовать перекрёстным ссылкам («см. аят N»);
современный автор как ЕДИНСТВЕННЫЙ далиль и обратная ссылка на классика — флаги.
Обкатано на `alyautdinov` (модернизм; ~30+ аятов; источник временно `hidden:true`).

## Рабочие правила
- Меняешь `config.json` → запусти `sync_config.py`. Меняешь данные источника →
  `compute_fill.py` И `build_coverage.py` (индекс покрытия для счётчика «Тафсиров: N»,
  списка у аята, режима источника и карты покрытия). Не редактируй `config.js`,
  `fill` и `coverage.json` вручную.
- `data/_sources/` в `.gitignore` — туда кладём сырьё/промежуточное, в коммит не идёт.
- Коммиты и changelog — по-русски, «новое сверху». Коммитить/пушить — только по
  просьбе пользователя.
- Для inline-обработчиков в HTML экранируй значения через `escAttr()`.
