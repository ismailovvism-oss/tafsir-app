# Руководство пользователя

Как пользоваться приложением. Этот же текст открывается **в самом приложении**:
меню **⋯** вверху справа → **❓ Справка** — с оглавлением, поиском по тексту и
без интернета. Справка одна, и это она: экран читает вот этот файл.

Для технических деталей и подготовки тафсиров — `GUIDE.md`.

---

## Новое в v4.5 — обновлённый экран чтения

Интерфейс чтения стал чище, все функции на месте:

- **Тап по номеру аята (или по ⋯ справа)** открывает **меню аята**: закладка,
  заметка, теги, **«Работа над аятом»**, диафильм, запись своего чтения,
  копирование (одного аята или диапазона) и список всех источников к аяту.
  У аята постоянно видны только **▶ слушать** и **☆ закладка**; если есть
  заметка или теги — рядом с номером значки ✏ и 🏷: **тап по ✏ открывает
  заметку**, **тап по 🏷 — теги** (прямые входы, без меню).
- **Басмала отдельной строкой.** «Бисми-Лляхи…» показывается над первым аятом
  суры, а не идёт первым словом внутри него, — как в печатном мусхафе. Аят
  копируется тоже без неё. В **аль-Фатихе** басмала остаётся на месте: там это
  настоящий первый аят; в **ат-Тауба** её нет вовсе. Выключается в ⚙ Вид.
  Так же и в **диафильме** (басмала — отдельной строкой над аятом) и в
  **проверке заучивания**: там она к тому же выдавала, что аят первый в суре,
  хотя вопрос «какая это сура?» скрыт.
- **Звук в проверке заучивания.** У точки входа и у каждого звена раскрытой
  цепочки есть **▶** — послушать этот аят и сверить себя с чтецом. Пока сура
  скрыта («Какая это сура?»), кнопок нет: плеер показывает адрес и выдал бы
  ответ. Нажатие ▶ у точки входа само открывает суру. В полноэкранном «Чтении
  наизусть» кнопка **▶ слушать** играет аят, который сейчас на экране.
- **Границы страниц мусхафа.** В ленте между аятами идёт полоса «Страница N» —
  там, где на печатной странице начинается новый лист. Удобно сверяться с
  бумажным мусхафом и учить «до конца страницы». Полоса стоит перед тем аятом,
  с которого страница начинается (на печати лист может начаться и посреди аята).
  Выключается в ⚙ Вид → «Раскладка переводов (Аяты)».
- **Подсмотреть другую сторону: кнопка `ع` или `RU`** рядом с ▶. Читаешь
  перевод и запнулся — тап по `ع`, и прямо под панелью появится арабский
  оригинал этого аята; повторный тап убирает. Читаешь оригинал — кнопка
  показывает `RU` и открывает перевод. Кнопка есть только тогда, когда
  включено что-то одно: если на экране и текст, и перевод, подсматривать
  нечего. Оригинал берётся из редакции «С огласовками», перевод — из первого
  включённого (а если ни одного не включено — из Кулиева).
- **«Работа над аятом»** — отдельный экран, где над одним аятом работают
  целиком: все включённые тафсиры и переводы подряд, рядом — инструменты
  **Заметка · Теги · Слова · Корни · Связи · Источники** (клавиши 1…6).
  Аят листается ‹ › или стрелками клавиатуры, выход — ✕, Esc или «Назад».
  - **На компьютере** видно всё сразу: слева список источников (галочка —
    включить/выключить, имя — перейти к его тексту; там же **«Есть у этого
    аята»** — что ещё можно добавить), в середине чтение, справа рабочая
    колонка. Заметка пишется, не теряя тафсир из виду.
    Источники разложены по разделам (переводы · тафсиры на русском · арабские);
    раздел показывает первые несколько, остальное открывает **«ещё N ↓»**, а
    тап по названию раздела сворачивает его целиком.
  - **На телефоне** экран отдан чтению, инструменты — в нижнем ряду; тап
    поднимает панель на половину экрана (потяните за ручку — на весь, тап по
    тому же значку — свернуть).
  - **Слова**: тап по слову — грамматика и корень; возьмите маркер — и тот же
    тап выделяет слово цветом. **Корни**: все корни слов аята с числом мест в
    Коране — тап открывает конкорданс. **Связи**: сколько источников толкуют
    этот аят, в какие темы указателя он входит и где вы на него ссылались.
  - **Сравнение**: кнопка **⇹ Сравнить** в списке источников ставит источники
    рядом — столько, сколько нужно. Набор выбирается в панели слева
    («☰ Источники»): поиск по названию, разделы (переводы · тафсиры на русском
    · арабские), тап по заголовку раздела берёт его целиком; на телефоне
    панель выплывает поверх текста.
    Ширина колонки подбирается по объёму текста: короткий перевод узкий,
    большой тафсир шире. Когда колонки перестают помещаться, они листаются
    вбок (на телефоне — свайпом). Каждая колонка прокручивается сама, поэтому
    короткий перевод остаётся перед глазами. **⤢ шире** отдаёт сравнению всю
    ширину, **✕ обычный вид** возвращает чтение.
  - Аят листается и **свайпом** влево/вправо (если свайпы включены в ⚙ Вид).
- **Клавиатура** (на компьютере): ←/→ — сура или страница, пробел —
  слушать/пауза, Esc — закрыть меню.
- **Шапка**: тап по названию **«Сура · аят ▾»** — быстрый переход (ввести
  «2:255», кнопки **⤒ Начало суры / ⤓ Конец суры**, недавние места, список
  сур). Справа: 🔍 поиск, 📖 переводы и тафсиры, **⋯** — прогресс, заучивание,
  Эрудит, аудио, вид и настройки, полноэкранный режим.
- **Панель чтения** под шапкой: 🏠 Главная · **Аяты | Мусхаф** · чип
  источников («Кулиев +1» — видно, что включено) · «Слои». При прокрутке
  вниз панель прячется, вверх — возвращается. Заучивание и Эрудит — на
  Главной и в меню ⋯; чтение по источнику — из панели 📚 («Карта покрытия»).

## Установка как приложения

В Chrome выбери меню **⋮ → Трансляция, сохранение и отправка →
Установить страницу как приложение**. На Android: **⋮ → Добавить на главный
экран → Установить приложение**. На iPhone/iPad: **Поделиться → Добавить на
главный экран → Добавить**.

Не выбирай **«Создать ярлык»**: современный Chrome открывает такой ярлык в
обычной вкладке. Если ярлык уже создан, удали его и установи приложение заново.

### Новые возможности приходят сами

Переустанавливать приложение после обновлений **не нужно**: установленное
приложение показывает ту же живую страницу, и всё новое появляется само —
достаточно закрыть его и открыть снова. Если новое не видно сразу, закрой
приложение из списка недавних и подожди несколько минут (сеть могла отдать
страницу из своего кеша).

Переустановка нужна только в одном случае — если сменились иконка или название на
домашнем экране и они не обновились сами.

---

## Работа без интернета

Приложение открывается и без сети. Само по себе оно офлайн умеет немного:
запуститься и показать то, что ты **скачал заранее**.

**⚙ Вид → Данные → «📥 Что скачать»**. Сначала выбери охват — **текущая сура**,
**текущий джуз** или **весь Коран**, — потом нажми ⬇ у нужного набора:

- **📖 Тексты, переводы и тафсиры** — только те источники, что сейчас включены в
  чтение (панель 📚). Выключенные не качаются, поэтому сначала включи всё, что
  хочешь читать офлайн.
- **🔤 Пословный разбор и корни** — слова с переводом, грамматика, конкорданс.
- **🎨 Цвета таджвида** — для текстовых начертаний (в печатном цвет уже в шрифте).
- **📕 Мусхаф: страницы и шрифты** — самый тяжёлый набор: у каждой страницы свой
  шрифт, примерно 80 КБ на страницу.

Внизу видно, сколько скачано и сколько места занимает приложение. Кнопка
**🗑 Очистить** удаляет всё скачанное — само приложение при этом останется.

Скачанное открывается **мгновенно**, потому что берётся с устройства: текст аятов
не меняется, перепроверять его незачем. Аудио пока не скачивается — чтение нужно
слушать по сети.

**На обновления офлайн-режим не влияет.** Код приложения всегда сначала
запрашивается из сети, и кеш подставляется, только когда сети нет.

Если что-то пойдёт не так, галочку **«Работать без интернета»** можно снять
(скачанное сохранится). Крайняя мера — открыть сайт с адресом, к которому дописано
**`?nosw=1`**: это полностью сбросит офлайн-слой.

---

## Панель чтения

Вверху, под шапкой — панель чтения: **🏠 · [ Аяты | Мусхаф ] · [чип
источников] · [Слои]**. При прокрутке вниз она прячется (экран — тексту),
при прокрутке вверх возвращается.

- 🏠 **Главная** — продолжить чтение, недавнее, быстрые ссылки, входы в
  Заучивание/Эрудит/Темы/Корни и **таблица Корана**: все 114 сур (или 30
  джузов) сеткой; порядок — мусхафный / хронологический (тартиб ан-нузуль) /
  по длине; фильтры структуры — Мекка/Медина, периоды длины, мукатта'ат,
  мусаббихат, пары сур. Тап по суре открывает её чтение.
- **Аяты** — лента: арабский аят + перевод(ы) + тафсиры, по блокам.
  Пословный разбор здесь же — галочкой «Со словами» (вкладка «Оригинал и
  слои»), а не отдельным режимом.
- **Мусхаф** — постраничный печатный вид (мусхаф Мадани): одна страница,
  15 строк. Только арабский текст. Пилюля **«Перевод аята ▴»** под страницей
  включает бокс перевода одного аята: листается ‹ ›, при прослушивании
  следует за чтецом, тап по адресу открывает меню аята; «Скрыть ▾» — убрать.
- **Чип источников** (например, «Кулиев +1») показывает, что сейчас включено,
  и открывает панель выбора текстов; **«Слои»** — редакции арабского, цвета
  таджвида, пословный разбор.

Куда делись остальные режимы: **Заучивание (хифз)** и **Эрудит** — на
Главной 🏠 и в меню ⋯; **чтение по источнику** — панель источников →
«🗺 Карта покрытия».

Переключение сохраняет место (на какой суре/странице был).

---

## Навигация (нижняя панель)

Внизу: **‹ [текущая позиция ▾] ›**

1. **Стрелки ‹ ›** — предыдущий / следующий по выбранной единице.
   - В режиме **Аяты** шагают по выбранной единице (сура/джуз/хизб/страница).
   - В режиме **Мусхаф** всегда листают **страницы** (показана одна страница).
2. **Тап по позиции** («18. Аль-Кахф» или «джуз 15 · …») открывает **единый
   навигатор** — тот же, что по тапу на название суры в шапке: переключатель
   **Сура · Страница · Джуз · Хизб** (он же задаёт, чем листают стрелки),
   на вкладке «Сура» — пара полей **[сура] : [аят]** (сура уже подставлена
   текущая: чтобы прыгнуть на аят своей суры, набери только его номер и
   Enter; аят пуст — переход к началу суры), на остальных вкладках — поле
   номера, сетки джузов/хизбов, начало/конец суры, недавние места.
3. **Свайп** влево/вправо по тексту — тоже листает.

Ширину боковой панели на компьютере можно менять: потяните за её правый край
(двойной клик по нему возвращает обычную ширину).

Слева — **боковая панель**. Вкладки у неё **столбиком слева** (значок с
подписью): **🔍 Поиск**, **📖 Суры** (список для глаз), **🏷 Теги**, **📑 Темы**,
**🌿 Корни**, **★ Закладки**, **📝 Заметки** и **⚙ Вид** (настройки). Поле
вверху работает по открытой вкладке: ищет суру, тему, корень, заметку — или, на
вкладке 🔍, по текстам переводов и тафсиров. Правило простое: панель — навигатор,
содержимое открывается в главном окне. У закладок, корней и заметок есть кнопка
«⛶ На весь экран» — полный список.

---

## ⚙ Вид — настройки отображения

Меню **⋯** вверху справа → **«Вид и настройки»** (или вкладка «⚙ Вид» в
боковой панели ☰) открывает настройки отображения.
Что внутри (в режиме «Аяты»):

| Настройка | Что делает |
|---|---|
| **Шрифт арабского** | Насх · Каллиграфический · Амири · Куфи · **Печатный** |
| **Размер шрифта** | Отдельно **Арабский**, **Русский** и **Тафсиры** |
| **Тема** | Светлая (по умолчанию) · Зелёная · Синяя · Золотая · Тёмная — красит всё приложение |
| **Межстрочный интервал** | Отдельно **Арабский** и **Русский** |
| **Поля** | Отступы по краям текста |
| **Боковая панель** | Размер названий сур и тегов |
| **Раскладка переводов** | ▤ Список (блоками) или ▥ Колонки (источники рядом) |
| **Оформление чтения** | Скины: Пергамент · Старая бумага · Тёмный манускрипт. Скин — «бумага с чернилами»: внутри ленты и акцентные элементы становятся охровыми, поэтому сочетается с любой темой |
| **Границы смысловых отрезков** | Включено по умолчанию. Тонкая черта со значком **ع** и названием темы между рукуʿ в ленте «Аяты» — деление Корана по смыслу, а не по страницам. Те же отрезки станок заучивания берёт порцией |
| **Картинки диафильма** | Набор фоновых картинок: Набор 1–4 или «Случайный» |
| **↺ По умолчанию** | Сбросить всё к исходному (шрифт → Печатный) |

Здесь «как выглядит» — включая шрифт. «Печатный» — глифы печатного мусхафа
Мадани (QPC), тот самый вид; печатные начертания действуют для редакции
«С огласовками». А «что читать» — редакции арабского и цвета таджвида — в
панели **📚 Источники** (блок «Оригинал», см. ниже).

В режиме **«Мусхаф»** доступен только межстрочный интервал и размер боковой
панели — размер текста и поля там подстраиваются под страницу автоматически.

---

## Переводы и тафсиры

Кнопка **📚** в шапке или **чип с именем перевода** в панели чтения — всё,
что ты читаешь. Панель разбита на **три вкладки**:

- **Выбрано** — включённые источники (и их порядок — кнопка «⇅ порядок»),
  «📝 Мои заметки» и **пресеты наборов**: «Только перевод», «Перевод +
  тафсир», «Учёба» (пословный разбор + таджвид). Твоя ручная подборка при
  применении пресета сохраняется в «Мой набор» — вернуть её можно одной
  кнопкой.
- **Каталог** — все переводы и тафсиры по группам; поиск сверху, уточнение —
  кнопка «Фильтры» (язык · вид · эпоха · направление).
- **Оригинал и слои** — редакции арабского, цвета таджвида и пословный
  разбор (сюда же ведёт чип «Слои» в панели чтения).

### Блок «Оригинал»
- **Редакции** (можно несколько сразу, можно выключить все — останутся одни
  переводы): с огласовками · без огласовок · без точек (расм — начертание
  времён сподвижников).
- **Цвета таджвида** — правила чтения цветом: мадды (оттенки красного),
  гунна/ихфа/иклаб (зелёный), калькаля (синий), непроизносимое (серый).
  Работает в **любом начертании**: в «Печатном» это цветное издание мусхафа
  1441 г.х., в остальных шрифтах — раскраска текста по посимвольной разметке
  правил. Касание окрашенной буквы показывает название правила с пояснением;
  рядом с галочкой — «легенда» цветов. Действует поаятно, пословно и
  постранично (для редакции «С огласовками»); в тёмной теме и скинах
  чтения цвета адаптируются.

- **Со словами (пословный разбор)** — разбивает редакцию «С огласовками» на
  **отдельные слова с переводом под каждым**; переводы и тафсиры остаются под
  словами, так что разбор слова доступен прямо при чтении тафсира. **Тап по
  слову** — карточка с грамматикой, леммой и **корнем** (тап по корню → все
  места Корана с этим корнем, см. «Корни слов»). Слова следуют начертанию из
  **⚙ Вид**: в «Печатном» это глифы мусхафа, в остальных шрифтах — обычный текст.
  **Цвета таджвида работают и на словах**: в «Печатном» цвет заложен в сам
  шрифт (нужен «Печатный (таджвид)»), в текстовых шрифтах слова красятся той
  же разметкой, что и сплошная строка.

- Длинные тафсиры **сворачиваются** — жми «Развернуть ▾».
- Тексты поддерживают оформление (жирный, таблицы, цитаты) — см. `GUIDE.md`.

### ⚠ Предупреждения об ошибках
- У некоторых переводов/тафсиров рядом с названием у аята может стоять значок
  **⚠** и цветная рамка — это редакторская пометка о возможной ошибке (сам
  текст источника **не меняется**). Цвет — по важности: **красный** —
  критическая, **жёлтый** — средняя, **синий** — допустимая.
- **Нажми на значок** — откроется пояснение: в чём ошибка и как правильно
  (бывает подробным). Предупреждение может относиться к конкретному аяту или ко
  всему источнику.

### Блок «🎧 Аудио» — слушать чтение
- Выбери чтеца и жми **«▶ Слушать текущую суру»**, либо нажми **▶ у номера
  аята** — чтение начнётся с него.
- Внизу появится **панель плеера** (встроена над навигацией, текст не
  перекрывает; на узком экране кнопки переносятся на вторую строку):
  - ⏮ ⏭ — по аятам, номер аята (тап — показать в тексте);
  - **🎧 Имя ▾** — выбор чтеца или своего набора записей прямо здесь;
  - **🔁 Повтор ▾** — тап открывает список: «Без повтора», «Повторять 2 раза /
    3 раза / бесконечно», **своё число повторов** (до 1000 — для заучивания)
    и **«Пауза после аята»** (1–10 секунд тишины, чтобы успеть повторить за
    чтецом; действует и между повторами); ниже «Повтор диапазона» — впиши
    номера **с … по …** (аяты текущей суры) и жми «Повторять»; сброс — там же;
  - **⏩ 1×** скорость: 1× → 1.25× → 1.5× → 0.75×;
  - **⌄** свернуть в узкую полоску (⌃ — развернуть), **✕** — закрыть.
- Текст сам следует за чтением: текущий аят подсвечивается и подкручивается,
  в «Мусхафе» перелистываются страницы. Смена чтеца или переводов не сбивает
  место чтения. Плеер виден и в режиме чтения (полноэкранном). Чтецы стримятся
  с everyayah.com — нужен интернет.

### 🎙 Запись своего чтения
- **Тап по номеру аята → «Записать чтение»** (или 🎙 в плеере) — записать своё
  чтение этого аята (браузер спросит доступ к микрофону). Откроется **лист
  снизу** (текст аята при этом подкручивается выше — читай его во время
  записи): **● Записать / Перезаписать**, **▶ Прослушать**, **🗑 Удалить**.
- Записи собираются в **наборы** («Мой голос», «Повторение»…) — создай набор
  в блоке «🎧 Аудио» (➕) и выбери его как источник (в панели 📚 или прямо в
  плеере через **🎧 ▾**): он играет через тот же плеер наравне с чтецами, аяты
  без записи пропускаются.
- Всё хранится **только на этом устройстве** и работает офлайн; записи никуда
  не отправляются и в облачную синхронизацию не входят. Удаление набора (🗑 в
  списке) стирает все его записи.

### 🖼 Картинки по темам и 🎬 диафильм (режим фокуса)
- **Полоса над текстом.** Галочка **«🖼 Визуальный ряд»** в блоке «🎧 Аудио»
  включает **атмосферную картинку над лентой**, подобранную по теме текущего
  аята (фихрист). Работает **отдельно от аудио**: можно просто читать — картинка
  меняется при переходе к следующему аяту; при прослушивании следует за чтецом.
- **🎬 Диафильм — полноэкранный режим фокуса.** Кнопка **🎬 в плеере**, пункт
  «Диафильм» в меню аята (тап по номеру) или вход с экрана 📊 Прогресс —
  открывает аят **на весь экран, по одному**,
  с атмосферным фоном по смыслу — ничего лишнего.
  - **Листайте свайпом ‹ ›** (или стрелками) без звука — спокойное
    сосредоточенное чтение. Либо включите чтеца — кадр меняется сам под чтение.
  - В кадре можно сменить **размер шрифта и положение текста** (**A / Aa+**),
    убрать картинку (**🖼**) или звук.
  - 📱 **Лучше смотреть в альбомной ориентации** — поверните телефон, картинка
    раскрывается полностью.
- **Наборы картинок.** В **⚙ Вид → «Картинки диафильма»** выберите набор фонов
  (**Набор 1–4**) или **«Случайный»** (свой набор на каждый вход).
- Фон — атмосферные природные кадры (рассвет, горы, вода, путь…); **сокровенное
  не изображается** — только природа и метафора. У аята без темы — нейтральный
  фон; кнопка **✕** выключает слой.

---

## Теги и заметки

- **Теги** — помечай аяты своими метками (вкладка «Теги» слева). Тап по
  **номеру аята → «Теги»**, чтобы повесить тег; выбери тег в панели — увидишь
  все помеченные аяты. Число тегов видно значком 🏷 рядом с номером.
- **Заметки** — включи «📝 Мои заметки» в каталоге; под аятом появится поле для
  своих мыслей. Сохраняется в браузере.
- **Несколько записей на аят** — размышление копится порциями, поэтому в
  «Работе над аятом» (инструмент **Заметка**) можно вести отдельные записи, у
  каждой свой вид: 💭 мысль · ❓ вопрос · ✔ вывод · 🤲 дуа · 🔗 связь, дата и
  свои теги. Пока запись одна, поле под аятом работает как раньше; когда их
  несколько, под аятом виден список, а правка — в верстаке.
- **Сборники** — тег, повешенный на отдельную запись. На экране «Замет.» →
  «⛶ На весь экран» есть переключатель **«по аятам | дневник»** (дневник —
  все записи по датам, новые сверху) и фильтры по виду записи и по сборнику.
- **🖊 Маркер слов** — меню аята → «Маркер слов»: четыре цветных маркера и
  ластик, тап по слову аята выделяет его. Выделения видны в ленте (в любом
  начертании, включая печатное), в пословном разборе и в «Работе над аятом»,
  синхронизируются между устройствами. Каждому маркеру можно дать **имя** (что
  он у вас значит): «Замет.» → «🖊 Мои выделения» → «✎ Названия». Список всех
  выделений — там же: показаны **сами выделенные слова**, фильтр по цвету,
  группировка по сурам, «⬇ .md» — скачать подборку файлом.
- **Размышление (тадаббур)** — «⤢ Развернуть» над полем заметки открывает
  полноэкранный редактор: сверху **сам аят с переводом** (тап по слову —
  вставить его в текст), кнопка **«☰ Каркас»** — четыре вопроса размышления
  (почему эти слова · что говорит обо мне · с чем связан · какое действие),
  адреса аятов в тексте(`2:255`) — кликабельные ссылки, **⬇ .md** — скачать
  заметку с аятом файлом (для Obsidian и любых внешних записей). В меню аята —
  «Копировать с заметкой» и **«Упоминания в заметках»** (в каких ваших
  записях процитирован этот аят). Весь список заметок сразу — «Замет.» →
  «⛶ На весь экран», там же «⬇ .md» (все заметки с аятами и переводом одним
  файлом), а адреса аятов внутри заметок кликабельны.

---

## Резервная копия и синхронизация

Твои личные данные — **заметки, закладки, теги и прогресс** — хранятся **в этом
браузере** (не на сервере). Почистишь данные браузера — они пропадут; на другом
устройстве их по умолчанию нет. Чтобы не потерять и переносить между устройствами
— два способа, оба в **⚙ Вид → «Резервная копия»**.

**1. Файл (есть всегда).**
- **⬆ Экспорт** — скачивает один `.json` со всем личным. Это и бэкап, и способ
  переноса: перекинь файл на другое устройство (мессенджер, облако, кабель).
- **⬇ Импорт** — загружает такой файл и **объединяет** с тем, что уже есть (не
  затирает). Если заметка к одному аяту различается — спросит: оставить свою,
  дописать обе или заменить.

**2. Облачная синхронизация.**
Блок «Синхронизация» с двумя кнопками — **🔑 Подключить** и **⟲ Синхронизировать**.
Никаких логинов и паролей: доступ к данным — по одному **коду синхронизации**.

Как связать два устройства:
1. На **первом** устройстве: ⚙ Вид → «Синхронизация» → **🔑 Подключить** →
   создаётся случайный код. **Запиши его** — это ключ к твоим данным.
2. Нажми **⟲ Синхронизировать** — личное уйдёт в облако.
3. На **втором** устройстве: ⚙ Вид → «Синхронизация» → **🔑 Подключить** →
   **введи тот же код** → **⟲ Синхронизировать**. Данные подтянутся и сольются.

- Код — он же ключ доступа: **храни его в тайне**, кто знает код — видит твои заметки.
- Дальше приложение синкает **полуавтоматически**: подтягивает данные при открытии
  и отправляет при уходе со страницы; кнопка ⟲ — для синка вручную в любой момент.
- Слияние **не удаляет** твоё: если удалил заметку на одном устройстве, она может
  вернуться при синхронизации с другого (это осознанный компромисс ради
  сохранности). Чтобы жёстко заменить — пользуйся файловым Импортом.

---

## 📊 Прогресс чтения

Меню **⋯** вверху → **«Прогресс чтения»**: дневная цель (минуты или аяты), серия дней с гуманным
восстановлением, недельная статистика, календарь активности и **карта покрытия
мусхафа** (какие из 604 страниц ты читал). Отсюда же — входы в «Проверку
заучивания» и «Скорость чтения».

---

## 🧠 Хифз — проверка заучивания

Вход: Главная 🏠 → «Заучивание», меню **⋯** → «Заучивание (хифз)» или с
экрана 📊. Это карта памяти + тренажёр:

- **Карта** — три уровня: джузы → суры → аяты («Назад» поднимает на уровень
  выше). Контур ячейки = твоя **декларация** («знаю / нетвёрдо / не знаю» —
  ставится сурой или тапом по блоку аятов), заливка = **подтверждено
  проверками**. Зелёное — закреплено, жёлтое — в работе, красное (на уровне
  аятов) — провалы. Сверху — те же фильтры структуры и порядки, что на Главной.
- **Охват проверки** — что именно проверяем. Два способа:
  - **Джуз** — джуз целиком, можно сузить до одной суры и до диапазона аятов в ней;
  - **Суры** — список сур строкой: `78-114` (весь муфассаль), `78-114, 36, 55`
    (диапазоны и отдельные суры вместе), `36` (одна сура). Так задаются охваты,
    которые в границы джуза не укладываются: муфассаль, «Аль-Бакара + Алю Имран»,
    свой набор выученных сур. Непонятное в строке приложение назовёт прямо
    («Не понял: «115»»), а остальное продолжит работать.
  Кнопка 🎯 в строке суры на карте берёт эту суру охватом целиком.
- **Проверка** — выбрав охват, жми «Начать»: приложение
  взвешенно выбирает слабое место, показывает аят-якорь (арабский или перевод,
  всегда с адресом сура:аят) — продолжи по памяти несколько аятов вперёд (или
  назад — сложный режим), раскрой цепочку и оцени каждый аят: «не помню /
  с трудом / уверенно» (ошибся кнопкой — просто нажми другую). Оценки строят
  расписание повторений (SRS): «закреплено» = уверенно + интервал от 3 недель;
  просроченное само возвращается в проверку.
- Проверка **не** засчитывается в дневную цель и серию (это отдельный флажок
  в настройках проверки).

---

## 📿 Станок заучивания — учить по программе

Вход: экран 🧠 **Хифз** → кнопка **«📿 Станок заучивания»**.

Проверка заучивания (выше) спрашивает то, что ты уже знаешь. **Станок — про
другое: он ведёт само заучивание.** Ты говоришь, какой кусок учишь и каким
методом, а приложение показывает порцию нужное число раз в нужном виде — с
текстом, с тающим текстом, по памяти — и само считает повторы. Твоё дело —
повторять вслух.

### Как начать: пять строк настройки

| Строка | Что задаёт |
|---|---|
| **Сура** и **Аяты** | какой кусок учим (например 78, аяты 1–5) |
| **Страница** | кнопка **«▤ взять страницу»** берёт страницу мусхафа целиком — ту самую бумагу, что видно в режиме «Мусхаф». Стрелки ‹ › листают страницы. Если страница лежит на двух сурах, приложение возьмёт её часть и скажет об этом |
| **Отрезок** | кнопка **«ع взять целиком»** берёт границы по смыслу, а не по счёту: это рукуʿ — деление, отмеченное значком ع на полях индо-пакистанских мусхафов. Стрелки ‹ › листают отрезки. Учить лучше законченной мыслью, а не «пятью аятами» |
| **Темп** | **тап** — листаешь сам; **звук + пауза** — станок ведёт сам (см. ниже) |
| **Отклик** | **голосом** (как в мактабе) или **первые буквы** — цифровой лоух (см. ниже) |
| **Показ** | **текст виден** · **гаснет** · **по памяти** — вуаль поверх любого метода, крутится и на ходу |

Ниже — список методов. Выбрал → **«Собрать программу и начать»**. Приложение
покажет план: сколько повторов, сколько из них связки, сколько примерно займёт.
Не понравилось — **«← Изменить»**.

### Что происходит на экране

Сверху — адрес порции и сколько повторов пройдено; полоска показывает прогресс.
Внизу написано, что делать сейчас: «прочитай вслух», «прочитай по памяти»,
«слушай и веди пальцем».

- **Тап в любом месте** — следующий повтор. Если текст скрыт, первый тап
  показывает его, второй ведёт дальше.
- **Клавиатура:** пробел или ← дальше, → назад, `1` `2` `3` — оценка,
  Esc — выйти.
- **♪** вверху — включить/выключить звук на ходу, **вуаль** — гасить текст,
  **‹** — вернуться на повтор назад, **✕** — закрыть.

Если у метода есть **ворота самооценки**, на повторах по памяти станок спросит
«Вспомнил?» — **не помню / с трудом / уверенно**. «Не помню» добавит ещё заход,
«уверенно» снимет остаток повторов этого шага. Занятие кончается не на N-м
повторе, а когда пошло без подсказки.

В конце — **«Отметить заученными»**: аяты появятся на карте 🗺 и встанут в
очередь проверки 🎯. Или **«Ещё раз»**.

### Темп «звук + пауза»

Читает **тот же чтец, что и в 🎧** (по умолчанию Мишари аль-Афаси) — это
намеренно: заученное под один голос под другим вспоминается хуже, менять голос
посреди блока значит частично учить заново.

Роль выбирается сама, по тому, что на экране:

- **текст виден** — чтец читает, потом тишина той же длины: повторяешь за ним;
- **по памяти** — тишина идёт **первой**: говоришь ты, а чтец читает следом и
  проверяет тебя.

Со звуком станок ведёт сам: отрезок доиграл — следующий. Тап обрывает.

Целый аят режется точно (у чтецов файл на каждый аят). Часть аята — швы,
нахлёсты — приближение по весу слогов; такие отрезки помечены неточными.

### Отклик «первые буквы» — цифровой лоух

На повторах «по памяти» вместо самооценки внизу появляется своя арабская
клавиатура (28 букв). Набираешь **первую букву каждого слова по порядку** —
верная проявляет слово целиком. Рука участвует в вспоминании, как на суданской
доске لوح, но без каллиграфии. Все варианты алифа — одна клавиша.

### Методы

Метод — это набор настроек: какой порцией брать, сколько раз показывать, в
каком виде, как склеивать соседние аяты.

**Традиционные** — школьные схемы хафизов:

| Метод | Суть |
|---|---|
| **Повторяй за чтецом** | аят три раза, четвёртый по памяти. **С чего начать** |
| **Сперва послушать** | мадинская программа: блок трижды на слух, водя пальцем, и только потом повторы |
| **Повтор двадцать** | классика: аят двадцать раз подряд |
| **Связка по четыре** | 20 раз аят, после каждых четырёх — все четыре вместе. Самая ходовая у хафизов |
| **По пять** | то же, связка каждые пять |
| **Три на три** | аят трижды, потом с соседом трижды |
| **Семь и три** | семь с текстом, три по памяти, потом связка |
| **Десять и десять** | для коротких рифмующихся сур |
| **Десять-три, потом всё** | в конце — весь блок целиком |
| **Шесть-четыре-четыре-шесть** | чередование; схема для детей |

**Швы между аятами** — лечат «каждый аят помню, а вместе рассыпается»:

| Метод | Суть |
|---|---|
| **Внахлёст** | аят + первые три слова следующего: стык попадает в середину порции, а не на край |
| **Внахлёст в обе стороны** | хвост предыдущего + аят + голова следующего; помогает начинать с середины |
| **Только швы** | голые стыки. Для выученного, которое сыпется на переходах |
| **Возвратное плетение** | к каждому шву возвращаются вперемешку с соседними — стык не остывает |
| **Сборка с конца** | учится последний аят, потом предпоследний вместе с ним, и так до начала: хвост блока перестаёт быть слабым. Читается при этом всегда по порядку |
| **Близнецы** | аяты, которые дословно повторяются в других сурах, ставятся рядом с двойником. Сперва видно только твой аят и адрес двойника — развилку надо назвать самому, тап открывает пару. Над каждым показан хвост предыдущего аята: у дословных близнецов различие именно там |

**Научные** — из исследований памяти:

| Метод | Суть |
|---|---|
| **Гаснущая подсказка** | текст пропадает по словам от повтора к повтору, последний — по памяти |
| **Через вспоминание** | один раз прочитал — пять раз вспомнил. Меньше чтения, больше усилия |
| **Разнесённый повтор** | повторы одного аята не идут подряд, между ними вклиниваются другие. Тяжелее, держится дольше |
| **Цифровой лоух** | набор первых букв (см. выше) |
| **Полезная трудность** | разнесённый + перемешанный порядок + минимум чтения. Самый неудобный и самый стойкий. Для **закрепления**, не для первого знакомства |
| **Синхронно с чтецом** | читаешь **вместе** с записью, а не следом: голос ведёт темп, протяжки и таджвид. Пауз нет, поэтому проходит быстро. Звук включается сам |
| **Страница целиком** | порция — не аят, а страница мусхафа целиком: двенадцать прочтений, последние по памяти. Так учат там, где счёт идёт страницами |
| **С конца блока** | аяты в обратном порядке: лечит «начало помню, конец плаваю» |

Часть методов в списке помечена **«Пока недоступны»** с причиной — они ждут
данных, которых в приложении ещё нет.

### Что станок намеренно НЕ делает

- **Не слушает микрофон.** Распознавание речи в приложении пробовали и убрали:
  оно ошибалось на таджвиде и наказывало за правильное чтение. Читаешь вслух —
  оцениваешь себя сам (или набираешь первые буквы).
- **Не ставит оценки в проверку 🎯.** Тренировка не экзамен: самооценка станка
  живёт отдельно и расписание повторений не двигает. В 🎯 попадает только
  «Отметить заученными» в конце.
- **Не идёт в дневную цель и серию** — как и проверка заучивания.

### Деление по темам в ленте чтения

Те же рукуʿ видно и при обычном чтении — **это включено по умолчанию**. В ленте
«Аяты» между отрезками идёт черта со значком **ع** и **названием темы**:
«Аят света; марево в пустыне», «Притча о двух садах», «Ночь предопределения».

**У каждой темы свой цвет.** Название стоит в цветной плашке, а вдоль аятов этой
темы идёт полоска того же цвета — по ней видно, докуда тема тянется и где
началась следующая. Цветов шесть, они идут по кругу, так что соседние темы
всегда разного оттенка. Насыщенность подобрана под тему оформления: на тёмной
краска светлее, на пергаменте приглушённее.

Это не то же, что полоса «Страница N»: страница — про бумагу, отрезок — про
смысл, поэтому и выглядят они по-разному. Выключить или вернуть — **⚙ Вид →
«Показывать границы смысловых отрезков»**.

Границы отрезков — канонические, из индо-пакистанских мусхафов. А **названия тем
писало приложение** по русскому переводу: это ярлык для навигации, не заголовок
от Аллаха и не часть Корана. Заметите неточность — её легко поправить.
---

## ⚡ Скорость чтения

С экрана 📊 → «Скорость чтения»: осознанные замеры беглости по страницам
мусхафа. Жми «Начать замер» — над страницей появится секундомер с явным
«Финиш» (при простое и скрытой вкладке он сам встаёт на паузу). По финишу —
время, WPM (слов в минуту) и сравнение с **личным рекордом этой страницы**;
недельный тренд — обзорный. Цель — беглость до комфортного уровня (задай
целевой WPM), не гонка на максимум. Замер засчитывается в дневную цель.

---

## Поиск

Кнопка **🔍** вверху (или Ctrl+F) открывает **вкладку поиска** в боковой панели:
результаты стоят рядом с текстом, а не поверх него, — видно и найденное, и тот
аят, от которого вы плясали. Арабский ищется без учёта огласовок. Галочка
«Искать во всех тафсирах» расширяет поиск за пределы включённых источников.

### 🎤 Найти аят голосом

Кнопка **«🎤 Найти аят голосом»** во вкладке поиска: прочитайте вслух несколько
слов аята (лучше 3–5 подряд) — приложение распознает чтение и покажет, какой это
аят; тап по результату открывает его. Распознавание работает **прямо на вашем
устройстве** (модель Тартиля, обученная на коранической тилаве): звук никуда не
отправляется. При первом запуске один раз скачается модель (~130 МБ — лучше по
Wi-Fi), дальше она берётся из кеша и работает офлайн. Пауза в полторы секунды
завершает запись сама; кнопка «⏹ Готово» — вручную.

### 🎤 Читать вслух — подсветка следит за чтением

Откройте суру в режиме **📖 Аяты**, затем в панели 📚 → блок «🎧 Аудио» нажмите
**«🎤 Читать вслух»**. Читайте с листа — после каждой фразы (короткой паузы)
приложение распознаёт прочитанное и передвигает подсветку на текущий аят,
подкручивая ленту. Внизу — пилюля с текущим аятом и последней распознанной
фразой; ⏹ останавливает. Распознавание на устройстве, звук никуда не уходит.

### 🤲 Чтение наизусть — листать аяты кнопками (без микрофона)

Кнопка **«по памяти»** в этом ридере — режим проверки: аят скрыт размытием,
вспомни его и прочитай вслух, первый тап показывает текст для сверки, второй —
следующий аят (снова скрытый).

Для чтения наизусть «не глядя»: панель 📚 → блок «🎧 Аудио» → **«🤲 Чтение
наизусть — листать аяты кнопками»**. Внизу появляется полоса с крупными
кнопками **«‹»** и **«аят ›»** — нажимайте не целясь: подсветка и лента
двигаются по аятам (границы сур переходятся сами). Запнулись → взгляните на
экран: текущий аят подсвечен по центру, экран при этом не гаснет. Кнопка 🎤
рядом — подстраховка: перечитайте запнувшуюся фразу вслух, голосовой поиск
найдёт аят и продолжит с него. Микрофон без 🎤 не включается.

### 🕶 Читать по памяти — проверка заучивания голосом

Там же — **«🕶 Читать по памяти»**: текст от текущего аята вниз скрыт (размыт),
читайте наизусть — прочитанный аят открывается. Запнулись — **💡** покажет
текущий аят (аят засчитается «с подсказкой»), **⏭** — «не вспомнил», аят
откроется и перейдём к следующему. Каждый аят записывается в тренажёр 🧠 Хифз
(SRS): «сам» / «с подсказкой» / «не вспомнил», в конце — сводка. Дочитали суру —
проверка завершится сама.

## Мои заметки — список

Вкладка **📝 Заметки** — все ваши заметки в одном месте (раньше найти свою
заметку можно было, только вспомнив аят). Тап уводит к аяту; поле вверху ищет и
по тексту заметки, и по названию суры. Кнопка «⛶ На весь экран» показывает
заметки целиком — с аятом, переводом и вашим текстом со всем оформлением.

## Корни слов (конкорданс)

Однокоренные слова в Коране связаны, и часто именно это открывает смысл: увидев все
места с корнем, вы видите, как слово живёт во всём Коране. Два входа:

- **Тап по слову** (при включённой галочке «Со словами») → в карточке разбора
  тап по **корню** → все места, где он встречается.
- **Вкладка 🌿 Корни** в боковой панели — указатель всех 1651 корня: тап по корню
  показывает его места в главном окне, так что корни можно перебирать один за
  другим, не теряя список из виду. Туда же ведёт ссылка «🌿 Корни слов» с Главной.
  Кнопка «⛶ На весь экран» раскрывает указатель во всё окно.

На экране корня: аяты с арабским и переводом, у каждого — **словоформа** пилюлей;
формы вверху работают фильтрами («показать только `ٱلرَّحِيمُ`» — 28 мест из 339).
Список выдаётся порциями, кнопка «Показать ещё» открывает следующие. Тап по аяту
уводит на него в ленте чтения.

**Искать корень можно по-русски** — набирать арабский не нужно: «милост» находит
`رحم` (339 мест), `صدق` (155), `نعم` (140), `فضل` (104) и другие. Подпись у корня —
черновой пословный глосс, то есть подсказка смысла, а не перевод.

Корень есть не у каждого слова: у частиц, местоимений и союзов (около трети
словоупотреблений) его нет и в самом корпусе — в указателе их не будет.

## Переход по номеру аята

Если вы знаете, куда идёте, искать по слову не нужно — есть два входа:

- **Строка поиска 🔍.** Наберите `2:255` (сура:аят) — первой строкой появится
  «➜ Перейти к аяту 2:255», тап или **Enter** ведут на него.
- **Панель «Суры»** (☰ слева) — поле **«К аяту»** под строкой поиска: номер и
  **Enter** (или кнопка ➜).

В обоих местах **голое число** — это аят **текущей суры**: читаете Аль-Бакару,
ввели `255` → попадёте на аят аль-Курси. Разделителем в `2:255` может быть
`:`, `.`, `-`, `/` или пробел. Несуществующий аят (например `2:999` — в
Аль-Бакаре 286 аятов) никуда не ведёт: строка перехода не появится, а поле в
панели мигнёт красным. Переход работает во всех режимах чтения; в **📕 Мусхафе**
открывается именно та страница, на которой стоит аят.

---

## Ссылки и «назад»
- Адрес вида `…#2:255` открывает суру 2, аят 255.
- Кнопка «назад» в браузере на телефоне закрывает открытую панель.

---

## Частые вопросы

**Почему печатный мусхаф «настоящий», а другие шрифты только похожи?**
Печатный (QPC) — это глифы, обведённые с печатной страницы (каждая страница —
свой шрифт). Остальные — обычные шрифты: красивые, но это самостоятельная
каллиграфия, не печатная страница.

**Всё пропало / выглядит странно после обновления.**
Обнови страницу с очисткой кэша: **Ctrl+Shift+R** (на телефоне — обновить
вкладку, при необходимости очистить кэш сайта).

**Работает ли офлайн?**
Шрифты и данные самохостятся; уже открытые страницы кэшируются браузером.
